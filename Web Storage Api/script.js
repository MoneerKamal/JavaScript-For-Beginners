// Web Storage API

// Not Part of the DOM, refers to the window API
// available to js via the gloable variable window

const myarray = ["item1", "item2", "item3"];

const myobject = {
  name: "moneer",
  arr: ["item1", "item2", "item3"],
};
